{
  "name": "blr-cherryblossom",
  "version": "1.0.0",
  "description": "Drop photos, bloom pink trumpet blossoms across Bangalore",
  "scripts": {
    "dev": "npx wrangler pages dev public --kv=DROPS_KV --r2=IMAGES_R2 --port 3000",
    "deploy": "npx wrangler pages deploy public --project-name blr-cherryblossom --branch main --commit-dirty=true",
    "setup:kv": "npx wrangler kv namespace create DROPS_KV",
    "setup:r2": "npx wrangler r2 bucket create blr-cherryblossom-images"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
